import React from "react";

const EmptyPage = () => {
    return (
        <div>
        <h1>No workouts to load right now!</h1>
        <h4> Use the dropdown menus mentioned above to render the possible workouts.</h4>
        </div>
    );
};

export default EmptyPage;